@page
@model ConfirmEmailModel
@{
    ViewData["Title"] = "Confirm email";
}

<h1>@ViewData["Title"]</h1>
@{
    <p>
        @Model.StatusMessage <span class="@Model.StatusHelper">Please <a asp-page="./Login">click here to log in</a>.</span>
    </p>
}
